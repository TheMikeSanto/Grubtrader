<%= form_for(@order_line) do |f| %>
  <% if @order_line.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@order_line.errors.count, "error") %> prohibited this order_line from being saved:</h2>

      <ul>
      <% @order_line.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :order_id %><br />
    <%= f.number_field :order_id %>
  </div>
  <div class="field">
    <%= f.label :inventory_id %><br />
    <%= f.number_field :inventory_id %>
  </div>
  <div class="field">
    <%= f.label :quantity_filled %><br />
    <%= f.number_field :quantity_filled %>
  </div>
  <div class="field">
    <%= f.label :quantity_requested %><br />
    <%= f.number_field :quantity_requested %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
